{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Launch",
            "program": "./a.out",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}
